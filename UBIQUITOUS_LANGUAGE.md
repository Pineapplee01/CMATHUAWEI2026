# Ubiquitous Language

## 核心任务

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **情感预测 / emotion prediction** | 根据文本、语音和视觉输入预测情感极性与连续情感强度。 | 只写“情感识别”、泛化为“情感分析” |
| **情感极性 / polarity** | 三分类输出 Negative、Neutral 或 Positive。 | sentiment class、类别标签 |
| **情感强度 / intensity** | 区间为 [-3, 3] 的连续情感标注或预测值。 | regression label、情绪分数 |
| **模态 / modality** | 文本、语音或视觉中的一种输入通道。 | channel（在领域文档中避免） |
| **多模态样本 / multimodal sample** | 由同一 video_id 和 clip_id 标识的三模态输入及其标签。 | 视频样本（当强调特征契约时） |

## 时序与缺失

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **时序对齐 / temporal alignment** | 将不同模态的时间位置映射到可比较的共同时间组织。 | 对齐（单独使用时含义不清） |
| **对齐版本 / aligned variant** | 附件2/3/4中三模态均按50个位置组织的特征版本。 | aligned data（未说明版本） |
| **非对齐版本 / unaligned variant** | 文本50个位置、语音和视觉最多500个独立时间位置的特征版本。 | raw data |
| **局部模态缺失 / local modality missingness** | 某模态的连续时间区间特征全部不可用或为零。 | 整模态缺失、missing modality（未说明范围） |
| **有效长度 / valid length** | 序列中未被填充且可用于模型计算的位置数量。 | sequence size、长度（未说明有效性） |
| **原生可用掩码 / native valid mask** | 预处理文件记录的原始内容或观测可用位置，允许包含非 padding 的特征缺失。 | valid mask（未说明来源）、padding mask |
| **合成缺失掩码 / synthetic missing mask** | Q2 实验为模拟连续局部缺失而新增的删除位置掩码。 | missing mask（未说明是原生还是实验） |
| **观测掩码 / observed mask** | 当前实验条件下实际送入模型的可观测位置，等于原生可用掩码与合成保留位置的交集。 | available mask、有效 mask |
| **物理支持 / physical support (P)** | 该模态在时间轴上可定义连续窗口的范围；不代表内容一定可观测。 | padding mask、valid mask（未说明语义） |
| **原生观测 / native observation (O)** | 输入中真实可用的内容位置；局部缺失仅从 O 中删除，且 O 是 P 的子集。 | P、padding（混用时） |
| **未对齐窗口化版本 / unaligned-windowed variant** | 对原始50/500/500输入先施加局部缺失，再以共享物理窗口池化为50槽的等长方法适配变体。 | 未对齐原生复现 |
| **源数据视图 / source data view** | 运行从 `processed_po` 读取的原始数据坐标系，只能是 `aligned_po` 或 `unaligned_po`。 | 输入视图、实验视图（未说明来源） |
| **模型输入视图 / model input view** | 模型实际接收的时间布局；对齐源只能是 `aligned_po`，未对齐源只能是 `unaligned_po` 或 `unaligned_windowed`。 | 源数据视图、对齐版本（未说明是否池化） |
| **Q2-v2 manifest** | 对 train/valid/test 的64个Q2条件、逐样本同步物理窗口、数据hash和mask hash的共享证据文件。 | 随机缺失列表、方法私有mask |
| **公平 Baseline run / fair baseline run** | 使用 Problem 2 双视图、共享Q2-v2 manifest、冻结方法决策规则、公共强度投影和统一产物校验的运行。 | 已有历史run、源码快照 |
| **Baseline Workspace** | 服务器 Baseline/reference 与本地 references 对应的源码、记忆和运行时工作区。 | reference 目录、方法文件夹 |
| **方法记录 / method record** | 方法目录中唯一描述方法身份、来源快照、adapter、能力和当前状态的条目。 | baseline 配置、准备度表 |
| **来源快照 / vendor snapshot** | 保留上游 Git 提交与许可证的第三方源码副本。 | 赛题适配、项目实现 |
| **运行记录 / run record** | 一个方法、视图和种子产生的检查点、预测、Q2 证据和验证状态。 | 日志目录、实验结果 |
| **结果摘要 / result summary** | 从已验证运行生成并受 Git 跟踪的紧凑指标和状态记录。 | 原始预测、checkpoint |
| **对齐测试252协议 / aligned test-252 protocol** | 对齐 test 划分上的252个连续局部缺失场景，固定使用七种模态组合、六个跨度和三次随机位置掩码。 | Q2-v2、随机缺失测试 |

## 解释与产物

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **可解释性 / explainability** | 模型输出能够以可量化、可复核的方式关联到输入证据。 | 因果解释 |
| **模态贡献 / modality contribution** | 某模态对当前预测结果的相对作用分数。 | 模态权重（除非确实是模型权重） |
| **关键证据 / salient evidence** | 与当前预测密切相关、可映射到文本、语音或视频位置的局部片段。 | 解释片段、原因（避免因果暗示） |
| **专项测试集 / special test set** | 附件3或附件4中无标签、只用于最终推理的样本集合。 | 测试集（与附件2 test 混用） |
| **数据契约 / data contract** | 规定输入字段、形状、掩码、标签和来源约束的公共接口。 | 数据格式 |

## 参考方法复现

| Term | Definition | Aliases to avoid |
| --- | --- | --- |
| **源码快照** | 从本地公开实现迁移并记录来源的代码版本。 | 已复现模型 |
| **赛题适配** | 保持核心方法并将输入、路径和评价接口接入赛题附件的修改。 | 原论文复现成绩 |
| **原生缺失协议** | 各论文实现自带的整模态或随机位置缺失实验规则。 | 赛题局部连续缺失 |
| **数据加载验证** | 在真实划分上检查原生加载器的字段、样本数和样本输出。 | 训练通过 |
| **训练复现** | 实际训练原实现并保存可验证的检查点与评价结果。 | 代码已迁移 |
| **不纳入复现** | 因赛题输入无法满足核心方法要求而退出当前对比的方法。 | 性能差、训练失败 |
| **中性区间校准** | 仅以验证集拟合回归输出的中性区间并冻结阈值后应用于测试和专项样本。 | 重新训练分类头、测试集调阈值 |
| **统一适配环境** | 为赛题参考方法共同安装并经过兼容检查的Python与依赖组合。 | 所有原论文的原版环境 |
| **连续区间评估** | 在原始序列坐标中删除某模态的连续位置区间并重新推理。 | 随机点缺失、整模态删除 |
| **删除重推理贡献** | 完整输入与删除某模态或局部窗口后的预测差异。 | 因果效应、模型权重 |
| **专项预测** | 对无标签附件3或附件4冻结模型的最终推理输出。 | 测试集评分 |
| **证据定位** | 与预测变化相关的模态位置、token范围或近似视频/音频时间范围。 | 情感原因、因果证据 |

迁移目录为服务器 `~/CPMCM/Baseline/reference`，每种方法独立维护。
附件2的单文件包装保留原始三划分和全部字段，不能混同重新划分或重新提取。
用户要求：无法满足赛题要求的方法直接退出复现，不用附件1的100条替代附件2训练集。
用户已确认：仅有回归输出的方法保留原模型和损失，通过中性区间校准输出三分类。
用户已确认：Q3贡献采用删除单模态/局部窗口后重新推理，输出归一化非负贡献与原始有符号变化。

## Reproduction relationships

- 一个 **源码快照** 可以对应一个 **赛题适配**；迁移和加载验证都不能代替 **训练复现**。
- **原生缺失协议** 与赛题连续局部缺失的结果分别报告。
- Graph-MFN 的 MMSA 实现与作者 DFG 组件分别标注来源，不能合称作者完整原实现。

## Relationships

- 一个 **多模态样本** 由一个 `video_id + clip_id` 唯一标识。
- 一个多模态样本包含文本、语音和视觉三个 **模态**，每个模态有序列值、有效长度和位置掩码。
- 一个样本可以同时拥有一个 **情感极性** 和一个 **情感强度**。
- 一个 **专项测试集** 属于问题2或问题3，只在最终推理阶段使用。
- 一个 **关键证据** 属于一个样本和一个模态，并具有起止位置与贡献分数。
- 一个 **方法记录** 可以关联零或一个 **来源快照**，并关联多个 **运行记录**。
- 一个 **结果摘要** 只汇总已通过公共验证的 **运行记录**。

## Example dialogue

> **Dev:** 这个样本是 aligned data 还是已经完成 temporal alignment？
>
> **Domain expert:** 前者表示文件版本，后者表示处理步骤；报告中分别写“对齐版本”和“时序对齐”。
>
> **Dev:** 附件3里的零值区间能叫 missing modality 吗？
>
> **Domain expert:** 只能叫局部模态缺失，因为整个模态仍然存在。
>
> **Dev:** 模型给出的高贡献片段就是情感原因吗？
>
> **Domain expert:** 不是。它是可复核的关键证据，不能直接宣称因果关系。

## Flagged ambiguities

- “情感识别”“情感分析”和“情感预测”在资料中混用；项目总称固定为 **情感预测**，文献原名除外。
- 附件2的 `classification_labels` 是数值编码，文档中的用户可读标签固定使用 Negative/Neutral/Positive。
- 附件2的 `test` 与附件3/4的无标签专项测试必须分开称呼，避免把有标签划分和最终推理集混淆。
