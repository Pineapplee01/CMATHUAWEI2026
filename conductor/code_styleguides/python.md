# Python Style Guide

- 使用 `snake_case` 文件名和绝对导入。
- 每个包的 `__init__.py` 显式声明 `__all__`。
- 公共类型放在 `contracts`，任务域不得依赖其他任务域的内部实现。
- 函数和公共数据类使用类型标注；数组形状和有效长度在边界处校验。
- 文件保持单一职责，测试放在平行的 `tests/` 目录。
- 不在 notebook 中保存唯一实现；可复现入口必须位于包或 CLI 中。
