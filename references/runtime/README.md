# Runtime Mirror

The executable server runtime is deployed to Baseline/reference/runtime from
the repository root. It contains the project package, Baseline queue scripts
and Baseline configuration only. Local development continues to use src,
scripts and configs at the project root.
