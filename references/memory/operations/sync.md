# Synchronization

Sync tracked runtime and memory files from the project workspace to
/Baseline/reference. Sync vendor snapshots only through fixed commits.
Never sync raw data, checkpoints, predictions, logs, caches or .venv.
