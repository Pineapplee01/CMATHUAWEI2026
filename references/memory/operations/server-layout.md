# Server Layout

The server workspace root is /user_home/gaojianan/CPMCM/Baseline.

- .venv contains the Python environment.
- reference/runtime contains the synced executable project subset.
- reference/vendor contains immutable third-party snapshots.
- reference/memory contains tracked Baseline knowledge.
- artifacts/problem2 contains raw runs and historical artifacts.
