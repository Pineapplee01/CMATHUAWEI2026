# Queue Policy

All runs use seeds 1, 2 and 3. A queue resolves adapters and output paths
through the method registry and artifact store. GPU 3 is the only Baseline
training device. A completed run requires public verification before it
contributes to a three-seed report.
